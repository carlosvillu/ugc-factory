Single-line text field with the standard surface-2 fill, accent focus ring, and an error state; use `mono` for data-like values.

```jsx
<Input value={url} mono onChange={handleUrl} placeholder="https://…" />
<Input value={price} error />
```
