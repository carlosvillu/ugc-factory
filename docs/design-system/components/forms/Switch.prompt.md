Boolean toggle — used for "Autopilot", "Firmar C2PA en export", and other per-setting flags. Solid accent track when on, outlined neutral track when off.

```jsx
<Switch checked={autopilot} onChange={setAutopilot} />
```
