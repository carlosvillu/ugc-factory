Multi-line field for descriptions, briefs, and per-scene script text. Same visual treatment as Input, vertically resizable.

```jsx
<Textarea rows={3} defaultValue={brief.description} onChange={handleEdit} />
```
