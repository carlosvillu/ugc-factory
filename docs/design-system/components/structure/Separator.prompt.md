Regla hairline de 1px `var(--border)` que divide contenido — la unidad de separación básica del sistema, horizontal o vertical.

```jsx
<Separator />
<Separator orientation="vertical" />
```

Notes: `role="separator"` con `aria-orientation` correcta. Solo el token `var(--border)`; nunca más de 1px de grosor.
