Native `<select>` styled to match Input, with a plain "▼" glyph caret (no icon asset — see readme Iconography).

```jsx
<Select value={goal} options={["Hook testing / awareness", "Conversión estándar"]} onChange={setGoal} />
```
