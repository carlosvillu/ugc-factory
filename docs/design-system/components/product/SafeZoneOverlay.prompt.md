Toggleable safe-zone guide over any 9:16 preview, so captions/overlays stay clear of platform UI. Pair with a segmented `Badge`/button group to switch `preset` (Universal/TikTok/Meta/Off) live, as in the canonical spec.

```jsx
<SafeZoneOverlay preset="universal" />
```
