Native range input (browser accent-color, not custom-drawn) with an optional label row showing the live value in mono at the right — used for e.g. render concurrency.

```jsx
<Slider label="Concurrencia de render" value={concurrency} min={1} max={8} onChange={setConcurrency} />
```
