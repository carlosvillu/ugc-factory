Underline tab bar for node side-panels and other secondary navigation (e.g. Brief / Guiones / Assets / Logs). Uncontrolled by default; pass `onChange` to observe selection.

```jsx
<Tabs tabs={["Brief", "Guiones", "Assets", "Logs"]} onChange={setActiveTab} />
```
